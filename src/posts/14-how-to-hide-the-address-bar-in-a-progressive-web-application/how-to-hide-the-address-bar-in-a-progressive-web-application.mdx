---
slug: '/how-to-hide-the-address-bar-in-a-progressive-web-application'
date: '2020-03-10'
published: false
title: '🔥 How to Hide The Address Bar in a Progressive Web Application'
tags: ['hot-tip', 'gatsby']
---

## Yes! Some code!

Here is the `Dump` component!

```jsx react-live
const Dump = props => (
  <div
    style={{
      fontSize: 20,
      border: '1px solid #efefef',
      padding: 10,
      background: 'white',
    }}
  >
    {Object.entries(props).map(([key, val]) => (
      <pre key={key}>
        <strong style={{ color: 'white', background: 'red' }}>{key} 💩</strong>
        {JSON.stringify(val, '', ' ')}
      </pre>
    ))}
  </div>
)

render(<Dump props={['One', 'Two', 'Three', 'Four']} />)
```
