---
category: 'gatsby'
slug: 'scroll-to-tag-gatsby'
title: 'scroll-to-tag-gatsby'
date: '2020-08-01'
published: false
---

<a href="https://github.com/CanRau/gatsby-scroll-bug/blob/master/gatsby-browser.js">
  article
</a>

Inside your `gatsby-browser.js` file:

```js
/* global window, document */
const scrollTo = id => () => {
  const el = document.querySelector(id);
  if (el) return window.scrollTo(0, el.offsetTop - 20);
  return false;
};

export const onRouteUpdate = ({ location: { hash } }) => {
  console.log(location);
  if (hash) {
    window.setTimeout(scrollTo(hash), 10);
  }
};
```
